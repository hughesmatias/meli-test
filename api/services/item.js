
class ItemService {
  getItem(id) {
    // call external service
    return false;
  };

  search(query) {
    // call external serfice
    return false;
  };
};

module.exports = new ItemService();
